environment: TEST

eudamed:
  TEST:
    base_url: "https://api.datalake.sante.service.ec.europa.eu"
    api_version: "v1.0"

    udi:
      path: "/eudamed/udi"
      format: "json"

  PROD:
    base_url: "https://api.datalake.sante.service.ec.europa.eu"
    api_version: "v1.0"

    udi:
      path: "/eudamed/udi"
      format: "json"
